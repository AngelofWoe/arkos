#!/bin/bash

main () {
    currbright=$(cat /sys/class/backlight/backlight/brightness);
    maxbright=255;

    echo $((200 * currbright/maxbright - 100 * currbright/maxbright ))
}

# Make sure script is running directly
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
