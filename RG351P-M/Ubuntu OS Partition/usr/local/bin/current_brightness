#!/bin/bash

main () {
    currbright=$(cat /sys/class/backlight/backlight/brightness);
    currbright=255;

    echo $((200 * currbright/currbright - 100 * currbright/currbright ))
}

# Make sure script is running directly
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
